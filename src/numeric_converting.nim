let
  a: float = 32.5
  b: int = 5

echo "Initial value of a is ", a
echo "Now, we will convert it to int"
echo "int(a) -> ", int(a)

echo int(a), " + ", b, " = ", int(a) + b

echo ""

echo "let's convert int to float! =)"
echo "we have b (", b, ")", " it is int for now"
echo "float(b) -> ", float(b)


